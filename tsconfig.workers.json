{
    "extends": "./tsconfig.json", // Herda do tsconfig principal
    "compilerOptions": {
      // Sobrescreve opções para os workers
      "noEmit": false,           // Habilita a emissão de arquivos JS
      "outDir": "./dist",        // Define a pasta de saída como dist/
      "module": "CommonJS",      // Usa CommonJS para compatibilidade com Node.js nos workers
      "moduleResolution": "node", // Geralmente usado com CommonJS
      "rootDir": "./lib",        // Define a raiz para calcular a estrutura em dist/
      "declaration": false,      // Não precisa gerar arquivos .d.ts para os workers
      "skipLibCheck": true,      // Mantém para acelerar
      "plugins": [],             // Remove plugins específicos do Next.js
      // Manter outras opções úteis herdadas como paths, target, esModuleInterop, etc.
    },
    "include": [
      // Inclui apenas os arquivos necessários para os workers e suas dependências em lib/
      "lib/workers/**/*.ts",
      "lib/redis.ts",
      "lib/db.ts",
      "lib/ai/**/*.ts",
      "lib/channel/**/*.ts",
      "lib/queues/**/*.ts",
      "lib/timeUtils.ts"
      // Adicione outros arquivos/pastas de lib/ que sejam dependência DIRETA dos workers, se houver
    ],
    "exclude": [
      // Exclui o que definitivamente não é necessário para os workers
      "node_modules",
      ".next",
      "app",          // Código da aplicação Next.js
      "components",
      "context",
      "public",
      "scripts",
      "prisma"        // Schema é usado em runtime, não precisa compilar aqui
      // Não precisa excluir 'dist' explicitamente se outDir for 'dist'
    ]
  }