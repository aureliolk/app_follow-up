# Arquivo de exemplo para configuracao de variaveis de ambiente
# Renomeie para .env e preencha os valores conforme seu ambiente
# Nao coloque dados reais de producao neste arquivo.

# --- Chave de criptografia utilizada para tokens e segredos ---
ENCRYPTION_KEY=

# --- Credenciais do Google OAuth ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# URL de callback registrada no Google Cloud Console
GOOGLE_REDIRECT_URI=http://localhost:3000/api/google-auth/callback

# --- Chaves para modelos de IA ---
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# --- Configuracoes do NextAuth ---
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# --- Variaveis expostas para o frontend ---
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_APP_URL=

# --- Configuracoes do Pusher (tempo real) ---
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=

# --- SMTP para envio de emails ---
SMTP_ADDRESS=
SMTP_PORT=587
SMTP_SSL=false
SMTP_USERNAME=
SMTP_PASSWORD=
MAILER_SENDER_EMAIL=

# --- Servico de armazenamento S3/Minio ---
STORAGE_ENDPOINT=
STORAGE_REGION=
STORAGE_ACCESS_KEY_ID=
STORAGE_SECRET_ACCESS_KEY=
STORAGE_BUCKET_NAME=
STORAGE_PUBLIC_URL=
STORAGE_FORCE_PATH_STYLE=false

# --- Servidor Redis ---
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# --- Integracao API Evolution (WhatsApp nao oficial) ---
apiUrlEvolution=
apiKeyEvolution=

# --- Webhook da API do WhatsApp oficial ---
WHATSAPP_APP_SECRET=

# Variavel de controle do ambiente (opcional: development ou production)
NODE_ENV=development
